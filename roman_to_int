#include <iostream>
#include <set>
using namespace std;

class Solution {
public:
    int find_index(int num, int<set> nums) {
        for (int i = 0; i < s; i++) {
            if (num == nums[i]) {
                return i;
            }
        }
        return -1;
    }
    
    int romanToInt(string s) {
        set<string> numerals = {"I", "V", "X", "L", "C", "D", "M"};
        set<int> values = {1, 5, 10, 50, 100, 500, 1000};
        
        for (int i = 0; i < s.length(); i++) {
        if (numerals.find(s[i]) != numerals.end()) {
                
            }
        }
    }
};
